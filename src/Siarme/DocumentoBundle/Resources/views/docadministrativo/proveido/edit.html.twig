{% extends 'DocumentoBundle:docadministrativo:layout_new.html.twig' %}

{% block tipodocumento %} PROVEIDO  {% endblock %}
{% block formulario %}
            {{ form_start(edit_form) }}
            {{ form_errors(edit_form) }}
            {{ form_row(edit_form.fechaDocumento, {'label': 'Fecha:'}) }}
            <div>
            {{ form_row(edit_form.dirigidoA, {'label': 'Se remiten las presentes actuaciones A:'}) }}
            {{ form_row(edit_form.texto, {'label': 'Motivo/Razon:'}) }}
            </div>
            <div class="text-right">
                    <input class="btn btn-danger"  type="submit" value="GUARDAR" />
            </div>
             {{ form_end(edit_form) }}
{% endblock %}
