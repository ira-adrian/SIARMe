{% extends 'base.html.twig' %}

{% block body %}
    <h1>Expediente</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expediente.id }}</td>
            </tr>
            <tr>
                <th>Letra</th>
                <td>{{ expediente.letra }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ expediente.numero }}</td>
            </tr>
            <tr>
                <th>Año</th>
                <td>{{ expediente.anio }}</td>
            </tr>
            <tr>
                <th>Extracto</th>
                <td>{{ expediente.extracto }}</td>
            </tr>
            <tr>
                <th>Observacion</th>
                <td>{{ expediente.observacion }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if expediente.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('backend_expediente_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('backend_expediente_edit', { 'id': expediente.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
